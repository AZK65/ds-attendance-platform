# Marketing site lives on Cloudflare Pages at qazidrivingschool.ca.
# This VPS only serves the ds-attendance platform under app.qazidrivingschool.ca.
# Caddy auto-issues + renews the Let's Encrypt cert.
app.qazidrivingschool.ca {
	encode gzip zstd
	reverse_proxy app:3000
}
