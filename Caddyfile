# Platform reachable at both the legacy root (qazidrivingschool.ca) and the
# new subdomain (app.qazidrivingschool.ca). Keep the root entry until the
# marketing site is fully cut over to Cloudflare Pages, then drop it.
qazidrivingschool.ca, www.qazidrivingschool.ca, app.qazidrivingschool.ca {
	encode gzip zstd
	reverse_proxy app:3000
}
